#1 list should be generated using list comprehension

fruits = ["apple", "banana", "cherry", "kiwi", "mango"]
newlist = []

for x in fruits:
  if "a" in x:
    newlist.append(x)

print(newlist)

# write a pyhon program to count the number of strings where the string length is 2 or more 
#'['abc','xyz', 'aba', '1221',]'

def match_words(words):
      ctr = 0

    for word in words:
        if len(word) > 1 and word[0] == word[-1]:
           ctr += 1
        return ctr

print(match_words(['abc', 'xyz', 'aba', '1221']))


#3 removing duplicated from the list using naive methods 
# fruits["Apple", "Banana", "melon", "Banana","Cherry","Banana",]
# initializing list 
sam_list = ["Apple", "Banana", "melon", "Banana","Cherry","Banana",]
print ("The list is: " + str(sam_list)) 

# remove duplicated from list 
result = [] 
for i in sam_list: 
    if i not in result: 
        result.append(i) 

# printing list after removal 
print ("The list after removing duplicates : " + str(result)) 


# 4 write a program to print a specified list after removing the 0th, 4th and 5th elements

color['Red', 'Green', 'White', 'Black', 'Pink', 'Yellow']
color = ['Red', 'Green', 'White', 'Black', 'Pink', 'Yellow']
color = [x for (i,x) in enumerate(color) if i not in (0,4,5)]
print(color)

# 5write a python program  to generate and print alist except for the first 5 elements , where the value are square of number between 1 and 30(both)

def printValues():
    	l = list()
	for i in range(1,31):
		 l.append(i**2)
print(l[5:])
printValues()